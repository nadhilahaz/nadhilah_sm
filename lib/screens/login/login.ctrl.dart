part of '_index.dart';

final ct = LoginCtrl();

class LoginCtrl {
  void kurang() {
    dt.rxHitung1.state = dt.rxHitung1.state - 1;

    // LoginData().rxHitung1.state = LoginData().rxHitung1.state - 1;
  }
}
